# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# .env overrides
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temp/scratch files that may contain secrets (MCP tool artifacts etc.)
*.tmp
*.temp

# Firebase CLI artifacts (may contain API keys)
firebase_*.tmp

# Secret / credential files — never commit these
secrets.json
credentials.json
service-account*.json
*-service-account*.json
stitch_mcp.json
